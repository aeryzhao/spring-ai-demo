logging:
  level:
    org.springframework.ai.mcp: DEBUG
    io.modelcontextprotocol: DEBUG

spring:
  application:
    name: mcp-server
  ai:
    mcp:
      server:
        enabled: true
        stdio: false
        name: comment-mcp-server
        version: 1.0.0
        type: SYNC
        instructions: 操作评论 MCP
        protocol: STREAMABLE
        streamable-http:
          mcp-endpoint: /mcp
          keep-alive-interval: 30s
        annotation-scanner:
          enabled: true

server:
  port: 8081
